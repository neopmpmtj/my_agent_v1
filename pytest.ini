[pytest]
DJANGO_SETTINGS_MODULE = conf.settings.test
python_files = tests.py test_*.py *_tests.py
markers =
    unit: in-process service/model tests (no HTTP, no call_command)
    integration: Django test client, management commands, or HTTP
